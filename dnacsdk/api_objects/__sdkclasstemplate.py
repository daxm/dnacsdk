"""
Template class to be inherited by all the other API object classes.

The point is to standardize and centralize as much of the configuration as possible.
"""

import logging
import json

logging.debug(f"In the {__name__} module.")


class ClassTemplate(object):
    """Base framework for all SDK modules."""

    pass
